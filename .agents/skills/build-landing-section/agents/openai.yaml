interface:
  display_name: "Build Landing Section"
  short_description: "Build one continuous editorial landing section"
  default_prompt: "Use $build-landing-section to implement one manifest-defined section with a content-derived composition and intentional neighbor transitions."
